public class LocalGlobalObjects {
    public static void varaibleScope (String [] arr) {
        //local = declared in a meathod visible only in the meathod
        //global = delcared outside a meathod but within the class, visible to all parts of the class
        LocalGlobalObjects diceRoller = new LocalGlobalObjects();

    }
}
